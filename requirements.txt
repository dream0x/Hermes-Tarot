# Hermes Oracle — runtime deps
# Pinned loosely; tighten before deploy if stability bites.

# LLM client (Kimi K2.6 is OpenAI-compatible)
openai>=1.40.0

# Image generation
fal-client>=0.5.0
Pillow>=10.4.0

# IPFS / HTTP
requests>=2.31.0

# Web3 (NFT mint on Base Sepolia)
web3>=6.20.0
eth-account>=0.13.0
py-solc-x>=2.0.0

# Config
python-dotenv>=1.0.0

# Telegram (used only if we run a standalone bot — Hermes has its own backend).
# The [job-queue] extra is REQUIRED for the daily-horoscope cron button.
# Without it, ctx.job_queue is None and tapping "Daily at 9 AM UTC" raises silently.
python-telegram-bot[job-queue]>=21.0

# Astro calc (lightweight; pyswisseph is heavier, skyfield is pure-python)
skyfield>=1.49

# Dev / testing
pytest>=8.0.0
